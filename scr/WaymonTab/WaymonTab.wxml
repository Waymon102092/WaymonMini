<view class="waymon_tab">
<van-sticky>
    <van-tabs active="{{ tabIndex }}" bind:change="onChange" >
      <block wx:for="{{tabs}}" wx:key="tab">
        <van-tab title="{{item}}"></van-tab>
      </block>
    </van-tabs>

</van-sticky>
</view>